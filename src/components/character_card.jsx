import CharacterAttributes from "./character_attributes"

export default function CharacterCard({ character }) {
    return (
        <div>
            <img src="#" alt=""/>
            <CharacterAttributes />
            <button>Edit</button>
        </div>
    )
}